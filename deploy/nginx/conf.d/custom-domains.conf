# Tenant custom domains for the combined stack (deelbot.com + *.deelbot.ai + extras).
# Same workflow as suhani/nginx/conf.d/custom-domains.conf — issue certs, paste
# server blocks here, reload deelbot-nginx.
#
#   cd deploy && ../suhani/scripts/issue-custom-domain-cert.sh you@email.com tenant.com www.tenant.com
#   (set COMPOSE_FILE=docker-compose.production.yml from repo root, or run certbot against deploy/certbot/www)
#
# Upstream name in this stack: suhani_app — use include /etc/nginx/snippets/suhani-tenant-server.inc;
